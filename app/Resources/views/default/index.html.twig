{% extends 'base.html.twig' %}
{% block body %}
    {% if current_user %}
        <div>Welcome, {{ current_user.name }}!</div>
    {% else %}
        <a href="{{ path('users_login') }}">Login</a>
    {% endif %}
    <div><a href="{{ path('books') }}">Books</a></div>
{% endblock %}